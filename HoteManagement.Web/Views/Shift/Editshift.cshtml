@model HoteManagement.Service.Model.ShiftDto

@if (Model != null)
{
   
<form>
    <input type="hidden" value="@Model.Id" id="editshift_id" />
    <div class="form-group">
        <label for="recipient-name" class="control-label">班次:</label>
        <input type="text" class="form-control" id="editshift_shiftname" value="@Model.shfit_name">
    </div>
    
    <div class="form-group" id="errormsg">
    </div>
</form>
}

